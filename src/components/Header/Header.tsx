import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <div></div>
            <h1>Funcionários</h1>
        </header>
    );
};

export default Header;